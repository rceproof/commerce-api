# 의존성 (npm install로 언제든 재생성 가능 — 용량 커서 올리지 않음)
node_modules/

# 환경변수·비밀정보 (API 키, 비밀번호 등 — 절대 올리면 안 됨)
.env
.env.local
*.pem
*.key

# 로그
*.log
npm-debug.log*

# OS·에디터 생성 파일
.DS_Store
Thumbs.db
.vscode/

# 빌드 산출물
dist/
build/
